---
phase: 02-state-capture-engine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/unit/test_capture.py
autonomous: true
requirements: [CAP-01, CAP-02, CAP-03, CAP-04, CAP-05, CAP-06, CAP-07]
must_haves:
  truths:
    - "A DummyLM subclass of dspy.clients.base_lm.BaseLM drives a real dspy.ReAct for a fixed iteration count with no network call"
    - "tests/unit/test_capture.py contains exactly 7 test functions mapping 1:1 to CAP-01..CAP-07, each invokable by its -k keyword"
    - "Each of the 7 tests is collected by pytest and fails (RED) for the right reason — missing TrajectoryCallback / unimplemented tracker, NOT an import or collection error"
  artifacts:
    - path: "tests/conftest.py"
      provides: "DummyLM test double + ReAct-with-tools harness fixtures"
      contains: "class DummyLM"
    - path: "tests/unit/test_capture.py"
      provides: "7 RED test stubs for CAP-01..CAP-07"
      min_lines: 80
  key_links:
    - from: "tests/conftest.py"
      to: "dspy.clients.base_lm.BaseLM"
      via: "DummyLM(BaseLM) subclass with forward() returning a FakeResponse carrying usage"
      pattern: "class DummyLM\\(BaseLM\\)"
    - from: "tests/unit/test_capture.py"
      to: "agent_router.tracker.TrajectoryTracker"
      via: "with TrajectoryTracker(session_id=...) wrapping a DummyLM-driven dspy.ReAct"
      pattern: "TrajectoryTracker\\(session_id"
---

<objective>
Build the Wave 0 test infrastructure for Phase 2: a network-free DummyLM test double that drives a
real `dspy.ReAct`, plus 7 failing (RED) test stubs in `tests/unit/test_capture.py` that map 1:1 to
CAP-01..CAP-07. These tests are the executable acceptance contract for the implementation work in
Plans 02 and 03 — they define exactly what "done" means before any production code is written.

Purpose: Nyquist compliance. Every implementation task in this phase verifies against these tests.
The phase cannot reach GREEN without them existing first, and they must fail for the right reason
(unimplemented capture), not for collection/import errors.

Output: `tests/conftest.py` (DummyLM + fixtures), `tests/unit/test_capture.py` (7 RED stubs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-capture-engine/02-VALIDATION.md
@agent_router/state.py
@agent_router/tracker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: DummyLM test double + harness fixtures in tests/conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (existing Phase-1 conftest — extend, do not clobber)
    - .planning/phases/02-state-capture-engine/02-RESEARCH.md §"DummyLM for tests (no network)" (lines 779-814) — the FakeResponse + DummyLM skeleton, verified against dspy 3.2.1
    - .planning/phases/02-state-capture-engine/02-RESEARCH.md §"Pattern 3: Token Usage Extraction" (lines 361-444) — the history-entry/usage/cache_hit shape the FakeResponse must satisfy
    - agent_router/state.py — TurnRecord/SessionState contracts the tests assert against
    - ~/.local/lib/python3.14/site-packages/dspy/clients/base_lm.py — BaseLM.__call__ / _process_lm_response, to confirm FakeResponse exposes .choices, .usage, .model, ._hidden_params
  </read_first>
  <action>
    Add a `DummyLM(BaseLM)` (subclass `dspy.clients.base_lm.BaseLM`, constructed with
    `model="dummy", cache=False`) plus a `FakeResponse` helper that mimics the LiteLLM response
    object `_process_lm_response` expects: a `.choices[0].message.content` (and `.tool_calls`),
    a `.usage` object exposing `prompt_tokens`/`completion_tokens` and iterable into a dict (so
    `dict(getattr(response, "usage", {}))` yields non-zero counts), `.model`, and `._hidden_params`
    with `response_cost`. `DummyLM.forward()` returns the next scripted FakeResponse, so the
    history `usage` path (lm.history[-1]["usage"]) is exercised with NON-ZERO tokens.
    Provide a scripted-response mechanism that lets a test drive a real `dspy.ReAct` for exactly N
    iterations: a tool function the ReAct can call plus a `finish` trigger on the Nth response, so
    the ReAct loop terminates after N react steps + 1 extract (matching the N+1 on_lm_end fire
    sequence in RESEARCH §"Pattern 2"). Add a way to script a cache-hit response (a FakeResponse
    with `usage = {}` and `cache_hit = True`) and an error response (forward() raises) so CAP-05 and
    CAP-06 tests can drive those paths. Provide a pytest fixture exposing a fresh DummyLM per call so
    isolation tests (CAP-07) can give each session its own LM instance (RESEARCH Open Question 2).
    Provide a fixture for a pre-existing dummy `BaseCallback` (records its own on_lm_end fires) so
    the CAP-02 preservation test has something to assert survived. Do NOT import fastembed/routellm.
    Keep `from __future__ import annotations` and the Phase-1 light-import discipline.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_imports.py tests/unit/test_contracts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from tests.conftest import DummyLM"` succeeds (or DummyLM is importable via the conftest fixture) with no network and no fastembed/routellm import
    - A throwaway snippet that runs `dspy.ReAct` with the DummyLM scripted for N iterations completes without hitting the network and produces `lm.history` entries whose `usage` dict has non-zero `prompt_tokens`/`completion_tokens`
    - Existing Phase-1 tests still pass: `python -m pytest tests/unit/test_imports.py tests/unit/test_contracts.py tests/unit/test_config.py -q`
  </acceptance_criteria>
  <done>conftest.py provides DummyLM (network-free, scripted, non-zero usage, cache-hit + error variants), a ReAct harness/tool, a per-call LM fixture, and a pre-existing-callback fixture; Phase-1 tests remain green.</done>
</task>

<task type="auto">
  <name>Task 2: 7 RED test stubs in tests/unit/test_capture.py (CAP-01..CAP-07)</name>
  <files>tests/unit/test_capture.py</files>
  <read_first>
    - tests/conftest.py (the DummyLM + fixtures from Task 1)
    - .planning/phases/02-state-capture-engine/02-VALIDATION.md §"Per-Task Verification Map" — the exact -k keywords each test must match (wrap, preserve_callbacks, signature_identity, overcount, tokens, exception, isolation)
    - .planning/ROADMAP.md §"Phase 2" success criteria 1-5 — the behavioral acceptance bars
    - .planning/phases/02-state-capture-engine/02-RESEARCH.md §"Pattern 2" (overcount: 5 iters → step_idx [0..4], 5 records), §"Pattern 4" (no StringSignature), §"Pattern 3" (non-zero tokens + cache_hit), §"Pattern 6" (exception path)
    - agent_router/state.py — TurnRecord fields to assert (step_idx, signature_name, input_token_count, output_token_count, cache_hit, exception)
  </read_first>
  <action>
    Write exactly 7 test functions, each selectable by the -k keyword in 02-VALIDATION.md:
    (1) `test_wrap` (CAP-01): a real `dspy.ReAct` driven by DummyLM runs UNCHANGED inside
    `with TrajectoryTracker(session_id="s1"):` and returns its normal prediction.
    (2) `test_preserve_callbacks` (CAP-02): register the pre-existing dummy callback via
    `dspy.context(callbacks=[pre_cb])` BEFORE entering the tracker, then assert pre_cb STILL fired
    (its on_lm_end counter > 0) during the tracked run — the dspy.context append-not-replace gate.
    (3) `test_signature_identity` (CAP-03): assert NO TurnRecord in the window has
    `signature_name == "StringSignature"`, and two distinct inline string signatures produce
    distinct `signature_name` values (the `StringSignature:<in>><out>` derived-key scheme).
    (4) `test_overcount` (CAP-04): a 5-iteration ReAct yields EXACTLY 5 TurnRecords with
    `step_idx == [0,1,2,3,4]` (max index 4, len == 5) — NOT 6, NOT 10-15; the trailing extract LM
    call is excluded.
    (5) `test_tokens` (CAP-05): every TurnRecord has non-zero `input_token_count` and
    `output_token_count`; a scripted cache-hit step is recorded with `cache_hit is True` and a
    distinct (cleared) token state rather than silently appearing as a normal zero-token call.
    (6) `test_exception` (CAP-06): when DummyLM.forward raises, a TurnRecord is still appended with
    `record.exception` populated (not None) and the `outputs=None` path does not crash the callback.
    (7) `test_isolation` (CAP-07): two `TrajectoryTracker` instances with different `session_id`
    (each its own DummyLM) run concurrently/sequentially and their windows contain only their own
    records — no step-count or window bleed; assert each session's window length and that
    `_SESSION_REGISTRY` is empty after both context managers exit.
    These are RED stubs: write the full assertions against the intended behavior. They MUST fail now
    because TrajectoryTracker/TrajectoryCallback are unimplemented — they MUST NOT fail at collection
    (no NameError/ImportError on the test module itself). Import TrajectoryTracker from
    `agent_router` / `agent_router.tracker`. Reference `_SESSION_REGISTRY` from `agent_router.state`.
    NEVER put fenced code blocks for the implementation here — these are tests, write real assertions.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_capture.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit/test_capture.py --collect-only -q` lists exactly 7 tests, no collection errors
    - Each -k keyword from 02-VALIDATION.md selects exactly one test: `pytest tests/unit/test_capture.py -k "wrap or preserve_callbacks or signature_identity or overcount or tokens or exception or isolation" --collect-only -q` lists 7
    - `python -m pytest tests/unit/test_capture.py -q` shows 7 FAILED (RED), with failures attributable to unimplemented capture (assertion/empty-window failures), not ImportError/NameError on the module
  </acceptance_criteria>
  <done>7 collectable, RED tests exist, one per CAP requirement, selectable by the documented -k keywords, failing for the right reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → dspy callback system | DummyLM and fixtures feed scripted data into dspy's in-process callback pipeline; no network, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | DummyLM scripted responses leak into other tests via shared state | mitigate | Per-call/per-fixture fresh DummyLM instance; no module-global mutable response list shared across tests |
| T-02-02 | Info disclosure | FakeResponse `content` (LM output text) captured into TurnRecord.output_text | accept | Test-only synthetic strings; no real PII/secrets in fixtures. Downstream PII exposure of output_text is a documented library-caller responsibility (RESEARCH §Security Domain), out of scope for tests |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced (RESEARCH §Package Legitimacy Audit: none); pytest/dspy already installed in Phase 1 |
</threat_model>

<verification>
- `python -m pytest tests/unit/test_capture.py --collect-only -q` → exactly 7 tests, 0 errors
- `python -m pytest tests/unit/test_capture.py -q` → 7 FAILED (RED, expected pre-implementation)
- `python -m pytest tests/unit/test_imports.py tests/unit/test_contracts.py tests/unit/test_config.py -q` → Phase-1 tests still green
- No fastembed/routellm import at conftest load time
</verification>

<success_criteria>
- DummyLM drives a real dspy.ReAct with non-zero usage, no network
- 7 RED tests, 1:1 with CAP-01..CAP-07, selectable by the 02-VALIDATION.md -k keywords
- Tests fail for unimplemented-capture reasons, never collection/import errors
- Phase-1 test suite unaffected
</success_criteria>

<output>
Create `.planning/phases/02-state-capture-engine/02-01-SUMMARY.md` when done
</output>
