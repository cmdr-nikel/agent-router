---
phase: 01-foundation-contracts
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - agent_router/state.py
  - tests/unit/test_contracts.py
autonomous: true
requirements: [LIB-01]
user_setup: []

must_haves:
  truths:
    - "TurnRecord exists, is frozen (immutable), and carries the D-06 telemetry fields"
    - "CostRecord exists, is frozen, and separates billed vs cache-free cost"
    - "SessionState exists, is mutable, and holds session_id, window, current_threshold, escalation_count, cost_log"
    - "The contracts pass mypy --strict on agent_router/state.py alone"
    - "TurnRecord has NO output_embedding field (it stores output_text only — Pitfall P2)"
  artifacts:
    - path: "agent_router/state.py"
      provides: "TurnRecord, CostRecord, SessionState, _SESSION_REGISTRY"
      contains: "class TurnRecord"
      exports: ["TurnRecord", "CostRecord", "SessionState"]
  key_links:
    - from: "agent_router/state.py"
      to: "collections.deque + threading.Lock"
      via: "SessionState.window (deque) + _lock field"
      pattern: "deque|Lock"
---

<objective>
Define the shared, fully-typed data contracts that all later blocks consume: the frozen TurnRecord
and CostRecord telemetry structs, the mutable SessionState, and the module-level session registry.
This is the single source of truth for the capture→scoring→routing seam.

Purpose: Satisfies ROADMAP success criterion 3 (contracts exist, fully typed, pass mypy --strict)
and locks decisions D-03 (mutability) and D-06 (field set). state.py imports NO heavy deps and NO
dspy, so it type-checks cleanly in isolation.
Output: agent_router/state.py with TurnRecord, CostRecord, SessionState, _SESSION_REGISTRY; the
contract assertions in tests/unit/test_contracts.py turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-contracts/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement TurnRecord, CostRecord, SessionState, and the session registry</name>
  <files>agent_router/state.py, tests/unit/test_contracts.py</files>
  <read_first>
    - agent_router/state.py (currently absent — created here) and tests/unit/test_contracts.py (the
      RED assertions written in Plan 01 that this task turns green)
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Code Examples → frozen dataclass for
      TurnRecord" (the exact field list) and §"Pattern 2: Frozen Dataclass" (the output_embedding
      prohibition)
    - .planning/phases/01-foundation-contracts/01-CONTEXT.md D-03 (mutability rules) and D-06 (field
      set for all three contracts)
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Common Pitfalls" P2 (no
      output_embedding on a frozen TurnRecord) and P3 (state.py must not import dspy so mypy --strict
      passes)
  </read_first>
  <behavior>
    - TurnRecord(...) constructs and is frozen: assigning to any field after construction raises
      dataclasses.FrozenInstanceError.
    - TurnRecord exposes exactly the D-06 fields: call_id, step_idx, signature_name, tool_name,
      tool_args, input_token_count, output_token_count, output_text, cache_hit, exception. It does
      NOT expose output_embedding.
    - CostRecord(...) is frozen and exposes call_id, model_used, billed_cost (Optional — None on
      cache hit), input_tokens, output_tokens, is_cache_hit.
    - SessionState(...) is mutable: appending to .window and reassigning .current_threshold /
      .escalation_count succeed without error. It exposes session_id, window, current_threshold,
      escalation_count, cost_log, and a private _lock.
    - _SESSION_REGISTRY is a module-level dict mapping session_id → SessionState.
  </behavior>
  <action>
    Implement agent_router/state.py per RESEARCH §"Code Examples → frozen dataclass for TurnRecord"
    using `from __future__ import annotations` (D-02), stdlib `@dataclass(frozen=True)` for TurnRecord
    and CostRecord, and a plain mutable `@dataclass` for SessionState. Use the EXACT field names and
    types from D-06 / RESEARCH: TurnRecord fields = call_id:str, step_idx:int, signature_name:str
    (class name + sorted field names — documented in a comment, not computed here), tool_name:str|None,
    tool_args:dict|None, input_token_count:int, output_token_count:int, output_text:str|None,
    cache_hit:bool, exception:Exception|None. CostRecord fields = call_id:str, model_used:str,
    billed_cost:float|None (None on cache hit — Pitfall P18), input_tokens:int, output_tokens:int,
    is_cache_hit:bool. SessionState fields = session_id:str, window:deque, current_threshold:float,
    escalation_count:int, cost_log:list, _lock:threading.Lock via field(default_factory=threading.Lock,
    repr=False). Add _SESSION_REGISTRY:dict[str,SessionState]={}. Do NOT add output_embedding to
    TurnRecord (Pitfall P2). Do NOT import dspy, fastembed, or routellm here (Pitfall P3 — keeps
    mypy --strict clean and the import light). Type-annotate the deque/list element types in comments
    or via typing where it does not break frozen/runtime behavior.
  </action>
  <verify>
    <automated>mypy --strict agent_router/state.py && python -m pytest tests/unit/test_contracts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `mypy --strict agent_router/state.py` exits 0 (ROADMAP success criterion 3)
    - `python -c "from agent_router.state import TurnRecord, CostRecord, SessionState, _SESSION_REGISTRY"` exits 0
    - A frozen-mutation attempt on a TurnRecord instance raises dataclasses.FrozenInstanceError (verified by a test in test_contracts.py)
    - A SessionState instance accepts `.window.append(...)` and `.current_threshold = 0.0` without error
    - `grep -n output_embedding agent_router/state.py` returns nothing (Pitfall P2)
    - `grep -nE "^import dspy|^import fastembed|^import routellm" agent_router/state.py` returns nothing (Pitfall P3)
    - `python -m pytest tests/unit/test_contracts.py -q` passes (the Plan 01 stub assertions go green)
  </acceptance_criteria>
  <done>state.py defines the three contracts with D-06 fields, correct mutability (D-03), no output_embedding, no heavy imports; mypy --strict clean; contract tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → SessionState | session_id is a caller-provided string (no validation in Phase 1) |
| concurrent sessions → _SESSION_REGISTRY | multiple sessions share one process-global registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-ID | Spoofing | caller-provided session_id collisions | accept | Phase 1 only defines the contract; uniqueness enforcement is documented as a Phase 2 caller responsibility (RESEARCH §Security Domain) |
| T-02-DoS | Denial of Service | _SESSION_REGISTRY grows unbounded | accept (Phase 1) | TurnRecord/SessionState are append-only structs; registry cleanup on __exit__ is a Phase 2 obligation — leave a documenting comment, no logic here |
| T-02-IMM | Tampering | telemetry mutated after capture | mitigate | TurnRecord + CostRecord are frozen=True (D-03) so captured telemetry is immutable under the concurrent sessions of CAP-07 |
</threat_model>

<verification>
- `mypy --strict agent_router/state.py` exits 0
- `python -m pytest tests/unit/test_contracts.py -q` passes
- `grep -n output_embedding agent_router/state.py` empty
- frozen-mutation raises FrozenInstanceError; SessionState mutation succeeds
</verification>

<success_criteria>
- LIB-01 (typed contracts): TurnRecord, CostRecord, SessionState exist, fully typed, mypy --strict clean
- D-03 honored: TurnRecord/CostRecord frozen, SessionState mutable
- D-06 honored: exact field sets present; CostRecord separates billed vs cache-free cost
- Pitfall P2 honored: no output_embedding on TurnRecord
</success_criteria>

<output>
Create `.planning/phases/01-foundation-contracts/01-02-SUMMARY.md` when done
</output>
