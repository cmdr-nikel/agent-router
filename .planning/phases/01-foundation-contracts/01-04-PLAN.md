---
phase: 01-foundation-contracts
plan: 04
type: execute
wave: 2
depends_on: ["01-02", "01-03"]
files_modified:
  - agent_router/tracker.py
  - agent_router/routing/dynamic_lm.py
  - agent_router/__init__.py
  - tests/unit/test_imports.py
autonomous: true
requirements: [LIB-01]
user_setup: []

must_haves:
  truths:
    - "from agent_router import TrajectoryTracker, DynamicRouteLM, RouterConfig succeeds"
    - "That import does NOT load fastembed, onnxruntime, or routellm into sys.modules"
    - "TrajectoryTracker is a usable context manager shell (enter/exit no-ops, no agent logic)"
    - "DynamicRouteLM is an importable dspy.BaseLM subclass stub (forward raises NotImplementedError)"
    - "The full public-API surface type-checks under mypy --strict"
  artifacts:
    - path: "agent_router/tracker.py"
      provides: "TrajectoryTracker context-manager stub (no fastembed import at module load)"
      contains: "class TrajectoryTracker"
      exports: ["TrajectoryTracker"]
    - path: "agent_router/routing/dynamic_lm.py"
      provides: "DynamicRouteLM dspy.BaseLM subclass stub (no routellm import at module load)"
      contains: "class DynamicRouteLM"
      exports: ["DynamicRouteLM"]
    - path: "agent_router/__init__.py"
      provides: "lazy public API re-export (RouterConfig eager; tracker/LM via PEP 562 __getattr__)"
      contains: "__getattr__"
  key_links:
    - from: "agent_router/__init__.py"
      to: "agent_router.tracker / agent_router.routing.dynamic_lm"
      via: "PEP 562 module-level __getattr__ deferred importlib.import_module"
      pattern: "__getattr__|importlib"
    - from: "agent_router/__init__.py"
      to: "agent_router.config.RouterConfig"
      via: "eager top-level import (core dep, always safe)"
      pattern: "from agent_router.config import RouterConfig"
---

<objective>
Complete the public API surface: the TrajectoryTracker and DynamicRouteLM stubs and the lazy
__init__.py that re-exports the three public names. The critical, subtle requirement is success
criterion 2 — `from agent_router import TrajectoryTracker, DynamicRouteLM, RouterConfig` must succeed
WITHOUT loading fastembed/onnxruntime/routellm at import time.

Purpose: Satisfies ROADMAP success criterion 2 (lightweight public import) and completes LIB-01's
"clean, documented public API surface". This is the connector that makes Blocks 2-4 buildable against
a stable import surface. Stubs contain NO block logic (capture/routing) — those are Phases 2 and 4.
Output: agent_router/tracker.py, agent_router/routing/dynamic_lm.py, the lazy agent_router/__init__.py;
test_public_api_import in tests/unit/test_imports.py turns GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-contracts/01-RESEARCH.md
@agent_router/config.py
@agent_router/state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement TrajectoryTracker and DynamicRouteLM stubs (no heavy imports)</name>
  <files>agent_router/tracker.py, agent_router/routing/dynamic_lm.py</files>
  <read_first>
    - agent_router/tracker.py and agent_router/routing/dynamic_lm.py (absent — created here)
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Code Examples → DynamicRouteLM stub"
      and §"Code Examples → TrajectoryTracker stub" (the exact stub shapes and signatures)
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Anti-Patterns to Avoid" (no
      fastembed import in tracker.py; no routellm import in dynamic_lm.py; do NOT use
      dspy.configure(callbacks=...) — leave a TODO for dspy.context())
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Open Questions" Q1 + §"Assumptions
      Log" A3 (DynamicRouteLM subclasses dspy.BaseLM in the Phase 1 stub; Phase 4 refines BaseLM vs LM)
    - .planning/research/ARCHITECTURE.md §"Public API Surface" (the documented constructor params for
      both classes)
  </read_first>
  <action>
    Implement agent_router/tracker.py: a TrajectoryTracker context-manager class per RESEARCH §"Code
    Examples → TrajectoryTracker stub" — __init__(self, session_id:str, config:Any|None=None),
    __enter__ returns self, __exit__ is a no-op. Add a `# TODO (Phase 2): create SessionState,
    register TrajectoryCallback via dspy.context() (NOT dspy.configure — Anti-Pattern), cleanup
    _SESSION_REGISTRY on __exit__` comment. Do NOT import fastembed/onnxruntime anywhere in this
    module (Anti-Pattern + success criterion 2). Implement agent_router/routing/dynamic_lm.py:
    DynamicRouteLM(dspy.BaseLM) per RESEARCH §"Code Examples → DynamicRouteLM stub" — __init__ with
    session_id:str, router:str="mf", routellm_base:str="http://localhost:6060/v1", api_key:str="",
    **kwargs; build an initial model string and call super().__init__(model=..., **kwargs); forward(...)
    raises NotImplementedError("DynamicRouteLM.forward implemented in Phase 4"). dspy IS a core dep so
    `import dspy` at module top is allowed (use `# type: ignore[import-untyped]`), but do NOT import
    routellm at module load (Anti-Pattern — it is under the [serve] extra). Use `from __future__
    import annotations` in both files (D-02). No block logic beyond the stub shells.
  </action>
  <verify>
    <automated>python -c "import sys; from agent_router.tracker import TrajectoryTracker; from agent_router.routing.dynamic_lm import DynamicRouteLM; assert 'fastembed' not in sys.modules and 'routellm' not in sys.modules; print('stubs-light-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from agent_router.tracker import TrajectoryTracker; t=TrajectoryTracker(session_id='x'); \nwith t as ctx: assert ctx is t"` exits 0 (context-manager shell works)
    - `python -c "from agent_router.routing.dynamic_lm import DynamicRouteLM"` exits 0
    - Importing both stub modules does NOT add 'fastembed' or 'routellm' to sys.modules
    - `grep -nE "import fastembed|import onnxruntime" agent_router/tracker.py` returns nothing
    - `grep -nE "^import routellm|^from routellm" agent_router/routing/dynamic_lm.py` returns nothing
    - `grep -n "dspy.configure" agent_router/tracker.py` returns nothing (Anti-Pattern avoided; TODO references dspy.context instead)
  </acceptance_criteria>
  <done>Both stubs import cleanly, TrajectoryTracker works as a context manager, DynamicRouteLM is a dspy.BaseLM subclass whose forward raises NotImplementedError, and neither module loads fastembed/onnxruntime/routellm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the lazy public API in __init__.py and turn the import gate green</name>
  <files>agent_router/__init__.py, tests/unit/test_imports.py</files>
  <read_first>
    - agent_router/__init__.py (minimal version from Plan 01 — replace with the lazy API; PRESERVE the
      literal `__version__ = "0.1.0"` line so hatchling's version path still works — Pitfall P4)
    - tests/unit/test_imports.py (the RED test_public_api_import assertion from Plan 01 this task
      turns green)
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Pattern 1: PEP 562 Module-level
      __getattr__" and §"Code Examples → PEP 562 module __getattr__" (the exact __init__.py shape)
    - .planning/phases/01-foundation-contracts/01-CONTEXT.md §specifics (the exact required import line)
  </read_first>
  <behavior>
    - `from agent_router import TrajectoryTracker, DynamicRouteLM, RouterConfig` succeeds.
    - After that import, sys.modules contains neither 'fastembed', 'onnxruntime', nor 'routellm'.
    - `agent_router.__version__ == "0.1.0"`.
    - Accessing an undefined attribute (e.g. agent_router.Nope) raises AttributeError.
    - Repeated access to TrajectoryTracker returns the cached object (no repeated importlib cost).
  </behavior>
  <action>
    Rewrite agent_router/__init__.py per RESEARCH §"Code Examples → PEP 562 module __getattr__":
    keep `from __future__ import annotations` and the literal `__version__ = "0.1.0"` (Pitfall P4);
    eagerly `from agent_router.config import RouterConfig` (core dep, always safe); define
    __all__ = ["TrajectoryTracker", "DynamicRouteLM", "RouterConfig"]; define _LAZY_MAP mapping
    "TrajectoryTracker" → "agent_router.tracker" and "DynamicRouteLM" → "agent_router.routing.dynamic_lm";
    implement module-level __getattr__(name) that importlib.import_module's the mapped module on first
    access, caches the resolved symbol into globals(), and raises AttributeError for unknown names.
    This guarantees `import agent_router` and even the named import never trigger fastembed/routellm at
    load time. Then make test_imports.py's test_public_api_import assert exactly the success-criterion-2
    behavior (named import succeeds AND fastembed/routellm absent from sys.modules) and any skip guards
    from Plan 01 are removed so it runs for real.
  </action>
  <verify>
    <automated>python -c "import sys; from agent_router import TrajectoryTracker, DynamicRouteLM, RouterConfig; assert 'fastembed' not in sys.modules and 'onnxruntime' not in sys.modules and 'routellm' not in sys.modules, sorted(m for m in sys.modules if m in {'fastembed','onnxruntime','routellm'}); print('lazy-api-ok')" && python -m pytest tests/unit/test_imports.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `from agent_router import TrajectoryTracker, DynamicRouteLM, RouterConfig` exits 0 (CONTEXT §specifics)
    - After the import, `'fastembed' not in sys.modules and 'onnxruntime' not in sys.modules and 'routellm' not in sys.modules` holds (ROADMAP success criterion 2; VALIDATION task 1-imp)
    - `python -c "import agent_router; assert agent_router.__version__=='0.1.0'"` exits 0 (Pitfall P4 — version path intact)
    - `python -c "import agent_router; agent_router.Nope"` raises AttributeError
    - `grep -n "__getattr__" agent_router/__init__.py` matches; `grep -n "__version__ = \"0.1.0\"" agent_router/__init__.py` matches
    - `python -m pytest tests/unit/test_imports.py -q` passes (test_public_api_import green)
    - `mypy --strict agent_router/` exits 0 (full package, with dspy/litellm overrides from pyproject)
  </acceptance_criteria>
  <done>The lazy public API exposes all three names, the named import loads no heavy deps, __version__ is intact for hatchling, the import gate test is green, and the whole package passes mypy --strict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import-time → optional extras | A stray top-level heavy import would silently pull torch/onnxruntime into core load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-IMP | Tampering | accidental heavy import at module load (fastembed/routellm) | mitigate | PEP 562 lazy __getattr__ + stub modules free of top-level heavy imports; test_public_api_import asserts sys.modules is clean (success criterion 2) |
| T-04-VER | Tampering | broken __version__ line breaks hatchling build | mitigate | acceptance criteria greps for the literal `__version__ = "0.1.0"` line (Pitfall P4) |
| T-04-DEF | Spoofing | unknown public-attr access silently returning wrong object | mitigate | __getattr__ raises AttributeError for any name not in _LAZY_MAP |
</threat_model>

<verification>
- `from agent_router import TrajectoryTracker, DynamicRouteLM, RouterConfig` succeeds with no fastembed/onnxruntime/routellm in sys.modules
- `python -m pytest tests/unit/test_imports.py -q` passes
- `mypy --strict agent_router/` exits 0 (full package)
- `python -c "import agent_router; assert agent_router.__version__=='0.1.0'"` exits 0
</verification>

<success_criteria>
- ROADMAP criterion 2: lightweight public import works without heavy deps (the subtle gate)
- LIB-01: clean, documented public API surface (TrajectoryTracker, DynamicRouteLM, RouterConfig)
- Stubs contain no block logic (Phase 2/4 boundaries respected)
- Full package passes mypy --strict
</success_criteria>

<output>
Create `.planning/phases/01-foundation-contracts/01-04-SUMMARY.md` when done
</output>
