---
phase: 01-foundation-contracts
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - agent_router/__init__.py
  - agent_router/routing/__init__.py
  - tests/conftest.py
  - tests/unit/test_imports.py
  - tests/unit/test_contracts.py
  - tests/unit/test_config.py
  - tests/integration/.gitkeep
  - tests/bench/.gitkeep
autonomous: false
requirements: [LIB-01]
user_setup: []

must_haves:
  truths:
    - "pip install -e . succeeds in the current env (hatchling build resolves)"
    - "The documented directory structure exists (agent_router/, tests/unit/, tests/integration/, tests/bench/)"
    - "pytest collects without import errors (config + asyncio_mode set)"
    - "Dev/build deps (hatchling, hatch, pydantic-settings, mypy, pytest, pytest-asyncio, pytest-mock) are importable"
  artifacts:
    - path: "pyproject.toml"
      provides: "hatchling build, core deps, optional extras (embed/serve/bench/dev), mypy + pytest config"
      contains: "hatchling.build"
    - path: "agent_router/__init__.py"
      provides: "package marker + __version__ for hatchling dynamic version"
      contains: "__version__"
    - path: "tests/unit/test_imports.py"
      provides: "failing/skeleton test stubs for lazy public-import gate (LIB-01)"
    - path: "tests/unit/test_contracts.py"
      provides: "failing/skeleton test stubs for contract shapes"
    - path: "tests/unit/test_config.py"
      provides: "failing/skeleton test stubs for RouterConfig fields + env override (LIB-02)"
  key_links:
    - from: "pyproject.toml"
      to: "agent_router/__init__.py"
      via: "tool.hatch.version path reads __version__"
      pattern: "tool\\.hatch\\.version"
    - from: "pyproject.toml"
      to: "tests"
      via: "tool.pytest.ini_options testpaths + asyncio_mode"
      pattern: "tool\\.pytest\\.ini_options"
---

<objective>
Establish the pip-installable package skeleton, the documented directory structure, the build/test
tooling, and the test scaffold that all later Phase 1 plans verify against. This is Wave 0: it
installs the missing dev/build dependencies and creates the structural foundation (pyproject.toml,
empty package, test dirs, test stubs) so that the contracts plan (02), the config plan (03), and the
public-API plan (04) can be implemented and verified.

Purpose: Satisfies LIB-01's "pip-installable (hatchling build)" and "documented directory structure"
success criteria, and lays the Nyquist test scaffold (every later task has an automated gate).
Output: pyproject.toml, agent_router/ + routing/ package markers, tests/ tree, test stub files,
installed dev deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-contracts/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dev/build dependencies (Wave 0 prerequisite)</name>
  <files>(environment only — pip/uv installs; no repo files modified)</files>
  <read_first>
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Standard Stack" (exact versions) and §"Environment Availability" (the 6 missing deps with no fallback)
    - .planning/phases/01-foundation-contracts/01-VALIDATION.md §"Wave 0 Requirements"
  </read_first>
  <action>
    Install the build/dev dependencies that are not yet present into the active python3.14
    environment: hatchling==1.30.1, hatch==1.17.0, pydantic-settings==2.14.1, mypy==2.1.0,
    pytest==9.1.0, pytest-asyncio==1.4.0, pytest-mock==3.15.1. fastembed 0.8.0 and routellm 0.2.0
    are already installed — do NOT reinstall them. Use pip3.14 (or `uv pip`) per RESEARCH §Standard
    Stack installation block. All 9 packages already passed the slopcheck Package Legitimacy Audit
    (RESEARCH §"Package Legitimacy Audit" — all [OK]); no additional legitimacy checkpoint needed
    beyond the blocking confirmation task below. Wrap each install in a timeout to avoid hangs.
  </action>
  <verify>
    <automated>python -c "import hatchling, pydantic_settings, mypy, pytest, pytest_asyncio, pytest_mock; print('deps-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import hatchling, pydantic_settings, mypy, pytest, pytest_asyncio, pytest_mock"` exits 0
    - `python -c "import pydantic_settings; print(pydantic_settings.__version__)"` reports 2.14.x
    - fastembed and routellm were NOT reinstalled (no torch/CUDA download occurred)
  </acceptance_criteria>
  <done>All six missing dev/build packages import cleanly; pre-installed fastembed/routellm untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1b: Package-install confirmation gate (supply-chain)</name>
  <action>Block on human confirmation that the dependency install pulled only the named packages and no unexpected heavy deps (especially torch via routellm). This is the package-install legitimacy gate for threat T-01-SC; it is never auto-approved.</action>
  <what-built>Installed 6 dev/build packages (hatchling, hatch, pydantic-settings, mypy, pytest,
  pytest-asyncio, pytest-mock) — all pre-rated [OK] by slopcheck in RESEARCH §Package Legitimacy
  Audit. This is the package-install confirmation gate (threat T-01-SC).</what-built>
  <how-to-verify>
    1. Confirm the install touched only the 7 named packages and their already-satisfied deps (no
       torch / CUDA / transformers download — those would indicate routellm was pulled into core).
    2. Spot-check one package page if desired: https://pypi.org/project/pydantic-settings/
    3. Confirm versions match RESEARCH §Standard Stack.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any unexpected package pulled in.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create package skeleton + directory structure + pyproject.toml</name>
  <files>pyproject.toml, agent_router/__init__.py, agent_router/routing/__init__.py, tests/conftest.py, tests/integration/.gitkeep, tests/bench/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Recommended Project Structure" and
      §"Pattern 5: hatchling flat-layout pyproject.toml (concrete shape)" (the full pyproject shape)
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Common Pitfalls" P1 (routellm under
      [serve]/[bench] only, NEVER in [project.dependencies]) and P4 (hatch version path needs literal
      __version__ string)
    - .planning/ROADMAP.md §"Phase 1" success criterion 5 (directory structure)
  </read_first>
  <action>
    Create the flat-layout package and test tree per RESEARCH §"Recommended Project Structure":
    agent_router/__init__.py (containing exactly the line `__version__ = "0.1.0"` plus
    `from __future__ import annotations`; full lazy public-API logic is added in Plan 04 — keep it a
    minimal valid module for now), agent_router/routing/__init__.py (empty package marker),
    tests/unit/, tests/integration/.gitkeep, tests/bench/.gitkeep, tests/conftest.py (empty shared
    fixtures module for now). Write pyproject.toml using RESEARCH §Pattern 5 verbatim shape:
    [build-system] hatchling>=1.30; [project] name="agent-router", requires-python=">=3.10",
    dynamic=["version"], core deps = dspy>=3.2.1, pydantic>=2.12, pydantic-settings>=2.14, openai>=2.0,
    numpy>=2.0; [project.optional-dependencies] embed=["fastembed>=0.8.0"], serve=["routellm[serve]==0.2.0"],
    bench=["routellm[serve,eval]==0.2.0"], dev=[mypy, pytest, pytest-asyncio, pytest-mock];
    [tool.hatch.version] path="agent_router/__init__.py"; [tool.hatch.build.targets.wheel]
    packages=["agent_router"]; [tool.pytest.ini_options] asyncio_mode="auto", testpaths=["tests"];
    [tool.mypy] plugins=["pydantic.mypy"], strict=true, python_version="3.10", files=["agent_router/"]
    with a [[tool.mypy.overrides]] for module=["dspy.*","litellm.*"] ignore_missing_imports=true; and
    [tool.pydantic-mypy] init_forbid_extra=true, init_typed=true, warn_required_dynamic_aliases=true.
    routellm MUST NOT appear in [project.dependencies] (Pitfall P1). The four extras (embed/serve/bench/dev) implement D-01's dependency layout: fastembed under [embed], routellm[serve] under [serve], eval/datasets under [bench]; core stays light. Do not embed any block logic.
  </action>
  <verify>
    <automated>test -f pyproject.toml && test -f agent_router/__init__.py && test -d tests/unit && test -f tests/integration/.gitkeep && test -f tests/bench/.gitkeep && grep -q '__version__' agent_router/__init__.py && grep -vq routellm <(grep -A8 '^\[project\]' pyproject.toml | grep -i 'dependencies' ) ; echo structure-ok</automated>
  </verify>
  <acceptance_criteria>
    - Directories exist: agent_router/, agent_router/routing/, tests/unit/, tests/integration/, tests/bench/
    - `agent_router/__init__.py` contains a literal `__version__ = "0.1.0"` line (no f-string/computed value, per P4)
    - `grep -n routellm pyproject.toml` shows routellm ONLY inside optional-dependencies (serve/bench), never in the core [project] dependencies list (P1)
    - pyproject.toml contains `build-backend = "hatchling.build"`, `requires-python = ">=3.10"`, and the four extras embed/serve/bench/dev
    - `[tool.mypy]` has `strict = true` and the dspy/litellm ignore_missing_imports override
  </acceptance_criteria>
  <done>Flat-layout package + test tree exist; pyproject.toml has hatchling build, correct extras (routellm optional only), mypy --strict config, and pytest asyncio_mode=auto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write test stubs (Nyquist scaffold) + smoke-install the package</name>
  <files>tests/unit/test_imports.py, tests/unit/test_contracts.py, tests/unit/test_config.py</files>
  <read_first>
    - .planning/phases/01-foundation-contracts/01-VALIDATION.md §"Per-Task Verification Map" (exact
      commands per task 1-imp / 1-ctr / 1-cfg) and §"Wave 0 Requirements" (which test files to stub)
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Validation Architecture" → "Phase
      Requirements → Test Map" (the named test functions: test_public_api_import,
      test_directory_structure, test_router_config_env, test_router_config_fields)
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Code Examples" for the expected field
      names of TurnRecord / CostRecord / SessionState / RouterConfig the stubs assert against
  </read_first>
  <behavior>
    - tests/unit/test_imports.py: test_public_api_import asserts that after
      `from agent_router import TrajectoryTracker, DynamicRouteLM, RouterConfig`, neither 'fastembed'
      nor 'routellm' is in sys.modules. (Will fail until Plan 04 wires the lazy API — that is correct
      RED state for Wave 0.)
    - tests/unit/test_contracts.py: test_directory_structure asserts the documented dirs exist;
      stub tests asserting TurnRecord/CostRecord are frozen and SessionState is mutable, with the
      D-06 field names present. (RED until Plan 02.)
    - tests/unit/test_config.py: test_router_config_fields asserts RouterConfig exposes the 6 D-05
      fields with defaults; test_router_config_env asserts an AGENT_ROUTER_WEAK_MODEL env var
      overrides weak_model. (RED until Plan 03.)
  </behavior>
  <action>
    Create the three unit-test stub files named above with the exact test-function names from
    RESEARCH §"Phase Requirements → Test Map" / VALIDATION §"Per-Task Verification Map". Each stub
    encodes the real assertions (do not write trivially-passing placeholders) so that later plans turn
    them GREEN by implementing the contracts/config/API — these are the goal-backward gates. Mark
    not-yet-implementable assertions so the suite collects cleanly (e.g. import-guarded skips that
    flip to real assertions once the symbol exists), but the import/structure tests should run now.
    After the stubs exist, run an editable install of the package to satisfy LIB-01's install gate.
    Use a timeout around the install to cap worst-case at seconds-to-a-minute. If `pip install -e .`
    cannot complete offline, fall back to `pip install -e . --no-build-isolation`.
  </action>
  <verify>
    <automated>pip install -e . >/tmp/ar_install.log 2>&1 && python -m pytest tests/unit -q --co && echo install+collect-ok</automated>
  </verify>
  <acceptance_criteria>
    - `pip install -e .` exits 0 (LIB-01 install gate; per VALIDATION task 1-pkg)
    - `python -c "import agent_router; print(agent_router.__version__)"` prints 0.1.0
    - `python -m pytest tests/unit -q --co` collects test_public_api_import, test_directory_structure, test_router_config_fields, test_router_config_env without collection errors
    - test_directory_structure PASSES now (dirs exist); contract/config/import assertions may be RED/skipped pending Plans 02-04
  </acceptance_criteria>
  <done>Editable install succeeds, pytest collects all named Phase 1 unit tests, and test_directory_structure is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local env | Untrusted package contents installed into the interpreter (supply-chain) |
| pyproject extras → installer | Misconfigured core deps could pull heavy/unexpected packages (torch via routellm) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pip installs (hatchling/pydantic-settings/mypy/pytest*) | mitigate | All 9 packages pre-rated [OK] by slopcheck (RESEARCH audit); blocking-human confirmation task after install verifies no unexpected package (esp. torch) was pulled |
| T-01-DEP | Tampering | routellm in core deps would pull torch (532 MB) | mitigate | routellm constrained to [serve]/[bench] extras only (Pitfall P1); acceptance criteria greps pyproject to assert it is absent from core deps |
| T-01-LOG | Information Disclosure | install logs | accept | Install log written to /tmp; no secrets involved in Phase 1 packaging |
</threat_model>

<verification>
- `pip install -e .` exits 0
- `python -c "import agent_router; print(agent_router.__version__)"` → 0.1.0
- `python -m pytest tests/unit -q --co` collects all four named tests with no errors
- `grep -n routellm pyproject.toml` shows routellm only under optional extras
</verification>

<success_criteria>
- LIB-01 (install): `pip install -e .` succeeds with hatchling build
- LIB-01 (structure): agent_router/, tests/unit/, tests/integration/, tests/bench/ all exist
- Dev/build deps installed and importable
- Test scaffold present and collecting; test_directory_structure green
</success_criteria>

<output>
Create `.planning/phases/01-foundation-contracts/01-01-SUMMARY.md` when done
</output>
