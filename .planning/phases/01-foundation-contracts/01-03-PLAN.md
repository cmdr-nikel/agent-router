---
phase: 01-foundation-contracts
plan: 03
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - agent_router/config.py
  - tests/unit/test_config.py
autonomous: true
requirements: [LIB-02]
user_setup: []

must_haves:
  truths:
    - "RouterConfig exposes the 6 D-05 fields with validated pydantic defaults"
    - "RouterConfig reads weak_model and strong_model from environment variables (D-04)"
    - "Out-of-range field values raise pydantic ValidationError"
    - "RouterConfig passes mypy --strict (with the pydantic.mypy plugin)"
    - "RouterConfig imports only pydantic-settings (a core dep) — always importable, no heavy deps"
  artifacts:
    - path: "agent_router/config.py"
      provides: "RouterConfig (pydantic-settings BaseSettings) with env binding"
      contains: "class RouterConfig"
      exports: ["RouterConfig"]
  key_links:
    - from: "agent_router/config.py"
      to: "environment variables"
      via: "SettingsConfigDict(env_prefix='AGENT_ROUTER_')"
      pattern: "env_prefix"
---

<objective>
Define RouterConfig, the validated, env-driven configuration object that exposes every tunable the
later blocks read (window size, thresholds, escalation cap, and the weak→strong model pair). This is
the config-driven model pair that LIB-02 requires.

Purpose: Satisfies ROADMAP success criterion 4 (the 6 fields with validated pydantic defaults) and
LIB-02 (weak→strong pair is config-driven), and locks D-04 (env-var source) and D-05 (the field set).
config.py imports only pydantic-settings (core dep), so the public API can always import it without
pulling fastembed/routellm.
Output: agent_router/config.py with RouterConfig; the field + env-override assertions in
tests/unit/test_config.py turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-contracts/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement RouterConfig (pydantic-settings BaseSettings) with env binding</name>
  <files>agent_router/config.py, tests/unit/test_config.py</files>
  <read_first>
    - agent_router/config.py (absent — created here) and tests/unit/test_config.py (the RED
      assertions written in Plan 01 — test_router_config_fields and test_router_config_env — that
      this task turns green)
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Code Examples → pydantic BaseSettings
      env binding" (the exact field list, defaults, and SettingsConfigDict) and §"Pattern 3"
    - .planning/phases/01-foundation-contracts/01-CONTEXT.md D-04 (env-var source for model pair +
      keys) and D-05 (the 6 required fields)
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Security Domain" (API keys are NOT
      stored in RouterConfig — only model-name strings; keys go to litellm/openai via their own env
      vars; use SecretStr only if a secret field is ever added)
    - .planning/phases/01-foundation-contracts/01-RESEARCH.md §"Common Pitfalls" P5 (asyncio_mode,
      already handled in pyproject) and §"Open Questions" Q2 (from __future__ import annotations vs
      pydantic runtime resolution — test instantiation to catch edge cases)
  </read_first>
  <behavior>
    - RouterConfig() constructs with no arguments and yields the documented defaults: window_size=10,
      default_threshold=0.11593, loop_similarity_threshold=0.85, max_escalations_per_session=3,
      weak_model="openai/gpt-4o-mini", strong_model="openai/gpt-4o".
    - Setting env var AGENT_ROUTER_WEAK_MODEL=foo/bar and constructing RouterConfig() yields
      weak_model == "foo/bar" (D-04 env override). Same for AGENT_ROUTER_STRONG_MODEL.
    - Constructing with window_size=0 (or any value below the lower bound) raises pydantic
      ValidationError; likewise default_threshold=2.0 (above 1.0) raises ValidationError.
    - `import agent_router.config` does not load fastembed, onnxruntime, or routellm into sys.modules.
  </behavior>
  <action>
    Implement agent_router/config.py per RESEARCH §"Code Examples → pydantic BaseSettings env
    binding". Use `from __future__ import annotations` (D-02). Define RouterConfig(BaseSettings) with
    model_config = SettingsConfigDict(env_prefix="AGENT_ROUTER_", env_file=".env",
    env_file_encoding="utf-8", case_sensitive=False). Fields (D-05, exact names + validated defaults):
    window_size:int = Field(default=10, ge=1, le=100); default_threshold:float = Field(default=0.11593,
    ge=0.0, le=1.0); loop_similarity_threshold:float = Field(default=0.85, ge=0.0, le=1.0);
    max_escalations_per_session:int = Field(default=3, ge=1); weak_model:str = "openai/gpt-4o-mini";
    strong_model:str = "openai/gpt-4o". Do NOT add API-key fields here — per RESEARCH §Security Domain,
    provider keys are handled by litellm/openai via their own env vars; RouterConfig holds only model
    identifier strings (if a secret field is ever introduced, use pydantic SecretStr to mask repr).
    Import only from pydantic and pydantic_settings — no dspy, fastembed, or routellm (keeps the
    public import light per success criterion 2).
  </action>
  <verify>
    <automated>mypy --strict agent_router/config.py && python -m pytest tests/unit/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `mypy --strict agent_router/config.py` exits 0 (pydantic.mypy plugin configured in pyproject)
    - `python -c "from agent_router.config import RouterConfig; c=RouterConfig(); assert {'window_size','default_threshold','loop_similarity_threshold','max_escalations_per_session','weak_model','strong_model'} <= set(c.model_fields); print('fields-ok')"` exits 0 (D-05)
    - `AGENT_ROUTER_WEAK_MODEL=foo/bar python -c "from agent_router.config import RouterConfig; assert RouterConfig().weak_model=='foo/bar'"` exits 0 (D-04 env override)
    - Constructing RouterConfig(window_size=0) raises pydantic ValidationError (verified in test_config.py)
    - `python -c "import sys, agent_router.config; assert 'fastembed' not in sys.modules and 'routellm' not in sys.modules"` exits 0
    - `python -m pytest tests/unit/test_config.py -q` passes (test_router_config_fields + test_router_config_env green)
  </acceptance_criteria>
  <done>RouterConfig exposes the 6 D-05 fields with validated defaults, reads the model pair from AGENT_ROUTER_-prefixed env vars (D-04), rejects out-of-range values, imports only core deps, and is mypy --strict clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS env / .env file → RouterConfig | Untrusted environment supplies model identifiers and (potentially) secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-ID | Information Disclosure | secrets leaking via RouterConfig repr/logs | mitigate | RouterConfig stores only model-name strings, NOT API keys (RESEARCH §Security Domain); provider keys are read by litellm/openai from their own env vars and never enter RouterConfig — so there is nothing secret to leak in Phase 1. If a secret field is ever added, use pydantic SecretStr (documented in action) |
| T-03-IV | Tampering | invalid config values (negative window, threshold > 1) | mitigate | pydantic Field(ge=..., le=...) validation raises ValidationError on out-of-range input (ASVS V5 input validation) |
| T-03-DEF | Tampering | insecure defaults | accept | Defaults are model-name strings + numeric thresholds with no security impact; no credential has a default |
</threat_model>

<verification>
- `mypy --strict agent_router/config.py` exits 0
- `python -m pytest tests/unit/test_config.py -q` passes
- env override of weak_model/strong_model works via AGENT_ROUTER_ prefix
- out-of-range field raises ValidationError; no heavy deps imported
</verification>

<success_criteria>
- LIB-02: weak→strong model pair is config-driven via env vars (D-04)
- ROADMAP criterion 4: all 6 D-05 fields with validated pydantic defaults
- RouterConfig imports only core deps (success criterion 2 support)
- ASVS V5: out-of-range values rejected by pydantic validation
</success_criteria>

<output>
Create `.planning/phases/01-foundation-contracts/01-03-SUMMARY.md` when done
</output>
